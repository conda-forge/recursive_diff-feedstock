{% set version = "2.1.0" %}

package:
  name: recursive_diff
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/r/recursive_diff/recursive_diff-{{ version }}.tar.gz
  sha256: fc2d3eee0fb84edc52e9fbc7a82f6ef04d51b92521f6484ac705d3356e2ff42b

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  entry_points:
    - recursive-diff = recursive_diff.cli.recursive_diff:main
    - ncdiff = recursive_diff.cli.ncdiff:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatchling
    - hatch-vcs
  run:
    - python >={{ python_min }}
    - numpy >=1.23
    - pandas >=1.5
    - xarray >=2023.8.0

test:
  imports:
    - recursive_diff
  commands:
    - recursive-diff --help
    - ncdiff --help
  requires:
    - python {{ python_min }}

about:
  home: https://github.com/crusaderky/recursive_diff
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Recursively compare two Python data structures

extra:
  recipe-maintainers:
    - crusaderky
